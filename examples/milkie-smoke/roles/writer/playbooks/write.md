Create a file named `result.json` inside the working directory you were given.

Use the `file_write` tool. The `path` argument MUST be the working directory path joined with `result.json` (an absolute path).

The file content MUST be exactly this JSON:

{"done": true, "note": "milkie provider smoke test"}

After writing the file, you are finished.
