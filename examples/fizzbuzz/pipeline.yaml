name: code-dev
description: Software development pipeline — design, develop, review

stages:
  - name: design
    roles: [designer]
    max_retries: 2

  - name: develop
    roles: [developer]
    max_retries: 5

  - name: review
    roles: [code_reviewer]
    max_retries: 2
