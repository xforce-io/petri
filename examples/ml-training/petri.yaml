providers:
  default:
    type: claude_code

models:
  haiku:
    provider: default
    model: haiku
  sonnet:
    provider: default
    model: sonnet

defaults:
  model: haiku
  gate_strategy: all
  max_retries: 2
