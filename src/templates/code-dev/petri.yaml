# code-dev defaults to Grok. To route a specific role elsewhere, add another
# named provider/model here and set `provider` + `model` in that role.yaml.
providers:
  default:
    type: grok

models:
  default:
    provider: default
    model: default

defaults:
  model: default
  gate_strategy: all
  max_retries: 3
