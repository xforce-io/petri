name: test
description: Test pipeline
goal: Test
stages:
  - repeat:
      name: work-loop
      max_iterations: 3
      until: work-approved
      stages:
        - name: work
          roles: [worker]
